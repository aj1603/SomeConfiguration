Configuration in github repisitory:
``` bash
echo "# SomeConfiguration" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Electronic12/SomeConfiguration.git
git push -u origin main
```
Git hub update command
``` bash
git remote add origin https://github.com/Electronic12/dgfdhgsr.git
git branch -M main
git push -u origin main
```

Github token: it may be change
```bash
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 

```



Codemy MIT teacher 

``` First create .ssh file on the computer 
creater@creater-pc:~/.ssh$ .ssh-keygen.exe             for windows machine
creater@creater-pc:~/.ssh$ ssh-keygen -t rsa           for linux machine


``` Second open id_rsa.pub and copy this file data
creater@creater-pc:~/.ssh$ cat id_rsa.pub

``` Third open github file on chrome open Setings, choose SSH and  GPG key create new SSH key
'ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABgQDeJT11U2oE6JFbbh2GsxReDF3sftiaRMEgRi0JzqMmSFcduhyIqg3RKr1...'


``` Forth open venv on the main file directory and those commands write in the terminal
git config --global user.name "Electronic12"
git config --global user.email "sowgatfilm@gmail.com"
git config --global push.default matching
git config --global alias.co checkout
git init
git add .
git commit -am 'initial commit'

``` Fifth create new reprository on the github account
git remote add origin https://github.com/Electronic12/flasker_sec.git
git branch -M main
git push -u origin main
git push


Codemy MIT teacher


```First on the .ssh file create .ssh-keygen
ssh-keygen -t rsa


``` Second on file .ssh oper the id_rsa.pub
cat id_rsa.pub


``` Third copy the data in the id_rsa.pub
.....


```Forth on is open git account and choose Setings, choose SSH and GPG keys and create New SSh key
....


```Fifth write the those five commands on the terminal in main file you should be in venv
git config --global user.name "Electronic12"
git config --global user.email "sowgatfilm@gmail.com"
git config --global push.default matching
git config --global alias.co checkout
git init 

```Sixth is Create new reprository in the git account
...

```Seventh write the commands on the terminal
git add .
git commit -am 'something'
git remote add origin https://github.com/Electronic12/flasker_sec.git
